/**
 * Сервис клавиатуры.
 *
 * В текущей версии inline-клавиатура создаётся напрямую через
 * Keyboard.inlineKeyboard() и Keyboard.button.link() из @maxhub/max-bot-api.
 *
 * Этот файл оставлен как точка расширения для будущих кнопок
 * (например, кнопки категорий товаров, callback-кнопки).
 */
export { Keyboard } from '@maxhub/max-bot-api';
