/**
 * Сервис сообщения с контактами.
 *
 * В текущей версии отправка контактов реализована в start.handler.ts
 * через ctx.reply() из @maxhub/max-bot-api.
 *
 * Этот файл оставлен как точка расширения.
 */
export { DEFAULT_CONTACTS_MESSAGE } from '../../content/messages';
